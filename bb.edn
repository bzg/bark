{:tasks
 {digest {:doc  "Digest emails into reports (use --all to rescan everything)"
          :task (load-file "scripts/bark-digest.clj")}
  bugs   {:doc  "List all bug reports"
          :task (do (def forced-command "bugs")
                    (load-file "scripts/bark-digest.clj"))}}}
