;; Minimal BARK config — fills in sensible defaults for :db (data/bark-db),
;; :logging (stderr :info) and :ingest (:fetch 50).  See config.edn.example
;; for the fully annotated template.

{:mailbox {:type :maildir
           :path "/path/to/your/Maildir"
           :folder "INBOX"}

 :sources [{:name "my-source"
            :list "list-id.example.org"}]}
